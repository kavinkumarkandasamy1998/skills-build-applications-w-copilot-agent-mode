OctoFit Tracker Backend

This folder contains a minimal Django project scaffold for local development.

Setup (once virtualenv activated):

```bash
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```
